name: Deploy Pipeline

on:
  push:
    branches:
      - develop
      - hom
      - main
  pull_request:
    branches:
      - develop
      - hom
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    name: Build and Test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build with Maven
        run: |
          cd financesb
          mvn clean package

      - name: Run Tests
        run: |
          cd financesb
          mvn test

  dev-environment:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop'
    name: Dev Environment Ready

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Dev Deployment Success
        run: echo "✅ Código em develop está pronto! Crie um PR para hom"

  hom-environment:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/hom'
    name: Hom Environment Ready

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Hom Deployment Success
        run: echo "✅ Código em hom está pronto! Crie um PR para main"

  prod-environment:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    name: Prod Environment Ready

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Prod Deployment Success
        run: echo "✅ Código em main foi para produção!"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ github.run_number }}
          release_name: Release v${{ github.run_number }}
          body: Automatic release from main branch
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}